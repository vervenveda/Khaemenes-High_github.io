"use strict";(()=>{const $=s=>document.querySelector(s);function calc(){const m=Number($("#mass").value),f1=Number($("#force1").value),f2=Number($("#force2").value),g=Number($("#gravity").value);if(![m,f1,f2,g].every(Number.isFinite)||m<=0||g<=0){$("#forceStatus").textContent="Enter a positive mass and gravitational field value.";return}const net=f1+f2,a=net/m,w=m*g;$("#netForce").textContent=net.toFixed(3)+" N";$("#acceleration").textContent=a.toFixed(3)+" m/s²";$("#weight").textContent=w.toFixed(3)+" N";$("#forceStatus").textContent=net===0?"Balanced horizontal forces: zero acceleration in this model.":"Unbalanced horizontal forces: acceleration has the net-force direction.";$("#forceEquation").textContent=`Fnet = ${f1} N + ${f2} N = ${net.toFixed(3)} N; a = Fnet/m = ${a.toFixed(3)} m/s²`;}document.addEventListener("DOMContentLoaded",()=>{$("#calculateForce").onclick=calc;calc()})})();