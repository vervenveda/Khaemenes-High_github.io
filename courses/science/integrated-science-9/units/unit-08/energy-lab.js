"use strict";(()=>{const $=s=>document.querySelector(s);function calc(){const ids=["emass","speed","height","gravityE","forceE","distanceE","timeE","inputE","usefulE"],v={};ids.forEach(id=>v[id]=Number($("#"+id).value));if(!Object.values(v).every(Number.isFinite)||v.emass<=0||v.gravityE<=0||v.timeE<=0||v.inputE<=0||v.usefulE<0){$("#energyStatus").textContent="Enter valid positive mass, gravity, time, and input energy.";return}const ke=.5*v.emass*v.speed*v.speed,gpe=v.emass*v.gravityE*v.height,work=v.forceE*v.distanceE,power=work/v.timeE,eff=v.usefulE/v.inputE*100;$("#ke").textContent=ke.toFixed(3)+" J";$("#gpe").textContent=gpe.toFixed(3)+" J";$("#work").textContent=work.toFixed(3)+" J";$("#power").textContent=power.toFixed(3)+" W";$("#efficiency").textContent=eff.toFixed(2)+"%";$("#energyStatus").textContent=eff<=100?"Energy account is numerically possible; evaluate system boundaries and uncertainty.":"Useful output exceeds input: revise the energy boundary or measurements."}document.addEventListener("DOMContentLoaded",()=>{$("#calculateEnergy").onclick=calc;calc()})})();