"use strict";(()=>{const $=s=>document.querySelector(s);function calc(){const depthPercent=Number($("#depthPercent").value),t1=Number($("#transit1").value),t2=Number($("#transit2").value),t3=Number($("#transit3").value);if(![depthPercent,t1,t2,t3].every(Number.isFinite)||depthPercent<=0||depthPercent>=100||!(t1<t2&&t2<t3)){ $("#transitStatus").textContent="Enter a depth between 0 and 100% and three increasing transit times.";return}const fraction=depthPercent/100,ratio=Math.sqrt(fraction),p1=t2-t1,p2=t3-t2,period=(p1+p2)/2,spread=Math.abs(p1-p2);$("#depthFraction").textContent=fraction.toExponential(4);$("#radiusRatio").textContent=ratio.toFixed(4);$("#periodDays").textContent=period.toFixed(3)+" days";$("#periodSpread").textContent=spread.toFixed(3)+" days";const planet=$("#transitPlanet"),size=Math.max(8,Math.min(100,ratio*220));planet.style.width=size+"px";planet.style.height=size+"px";$("#transitStatus").textContent=spread<=Math.max(.02*period,.01)?"The intervals are mutually consistent in this simplified record. Repetition supports periodicity but false-positive analysis is still required.":"The intervals differ meaningfully; inspect noise, missed events, timing uncertainty, or a nonplanet explanation."}document.addEventListener("DOMContentLoaded",()=>{$("#calculateTransit").onclick=calc;calc()})})();